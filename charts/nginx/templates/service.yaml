harsha_c2k4@cloudshell:~/helm-gitops/charts/nginx/templates (harsha-503911)$ cat service.yaml 
{{- if .Values.application.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ .Release.Name }}

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
{{- end }}
harsha_c2k4@cloudshell:~/helm-gitops/charts/nginx/templates (harsha-503911)$ 
